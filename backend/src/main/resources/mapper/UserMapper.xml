<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.sqltuning.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.example.sqltuning.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="role" column="role"/>
    </resultMap>

    <select id="findByUsername" resultMap="UserResultMap">
        SELECT id, username, password, role
        FROM users
        WHERE username = #{username}
    </select>

    <insert id="insertUser" parameterType="com.example.sqltuning.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password, role)
        VALUES (#{username}, #{password}, #{role})
    </insert>

</mapper>
